model Course {
    title       String
    description String?
    image       String?
    categoryId  Int
    category    Category @relation(fields: [categoryId], references: [id])
    topics      Topic[]
    points      Int      @default(0)
    ...boilerplate
}
